# E1 — CKA / SVCCA / Procrustes-distance 同場 ranking 比較

## 1. 目的與回應對象

| Reviewer 點 | 內容 |
|---|---|
| **G3T9-W3**(主要) | 「論文定位是比 CKA/SVCCA 更好,卻沒有同場直接比較」——rebuttal 策略文件標記為 P0、**核心定位 claim、無備案必做** |
| pCi8-W3 | 「Ω 單獨用 vs 完整 PRISM 差距小」——本表同時提供 1−Ω_I 欄位,支撐「Ω 是由 bound 推導出的正確相似度」的論證 |
| Global response | 新增比較表是六項新實驗的第一項 |

填入 Google Doc G3T9-W3 草稿中的佔位符:
`PRISM [XX], linear-CKA [XX], SVCCA [XX], Procrustes distance [XX]`。

## 2. 可行性結論:**可行,無需犧牲**

- **成本估計:每個 family 約 1.5–2.5 小時**(RTX 5090)。兩個 family 合計 **~3–5 h**。
  - 瓶頸是 proxy 重新載入(GGUF dequant 每個 ~2–5 分鐘 × ~12 variants),特徵抽取本身很快(512 樣本 × 5 benchmarks ≈ 每個 variant 2–3 分鐘)。
  - 前提:5090 機器上 HF cache 已有這些 GGUF/GPTQ 檔(跑過論文量化實驗的機器都有)。
- **取捨(與論文主文一致,非妥協)**:只跑 **Llama-3.1-8B + Qwen3-8B-Base**(主文 2×5 grid 的兩個 family)。Ministral / DeepSeek replication 在 rebuttal 中承諾 revision 補上——這與論文本身 main/appendix 的切分一致,reviewer 無法以此挑剔。

## 3. 實驗設計

- **Variant 清單直接由論文 CSV 解析**(`exp_result/quantization/quantization_merged_slim.csv` 的 `proxy_model` 字串),保證與論文完全同一組 proxies(Llama:FP16、Q8_0…Q2_K、INT8/NF4/FP4、2×GPTQ,共 12)。
- **同 protocol**:同 tokenizer、同 dataloader(512 樣本、max_length 512、seed 42)、同 token-level `concat` 特徵。target 特徵抽一次後以 fp16 cache 到磁碟(`out/E1/{family}_ZT/`),中斷可續跑。
- 在**同一批 paired token 特徵**上計算:
  - `cka` — linear CKA(centered)
  - `svcca` — 99% variance PCA(上限 256 維)後 CCA 的 mean canonical correlation
  - `procr_dist` — size-and-shape Procrustes distance(O(d) 對齊後殘差 /√n)
  - `omega_I` — PRISM trace 相似度(與論文表格的 sanity link)
  - `iso_dev` — **E8(pCi8-W6)搭便車**:無約束最優對齊 A* = argmin‖Z_T A − Z_P‖ 的奇異值 CV;0 ⟺ 恰為 scaled isometry,隨激進量化的非等向扭曲上升。零額外 GPU,直接回答「near-isometry 在激進量化下還成立嗎」的量化版
- CKA/SVCCA 在 16,384 個成對 token 上計算(`--token_cap`,同 index 兩側對齊、seed 固定)——CKA/SVCCA 在 >10k 樣本已極穩定,且避免 100k×4096 的 SVCCA 成本。
- `|ΔR|` 直接從論文 CSV join(`(Label, dataset)` 對 `|MdR|`),因此 Spearman 表與論文 Fig. 2 / Table 3 的 PRISM 數字**可直接並排**。
- **PRISM B 欄同表內算**:`Bound_I` 也從 CSV join,rs 在**與 baseline 完全相同的 variant 子集**上計算——即使某 proxy 載入失敗,五欄仍同 n,可比性不被挑(引用論文 0.820 時另註 anchor)。
- **符號慣例**:以 degradation score(Bound_I、1−CKA、1−SVCCA、procr_dist、1−Ω_I)對 `|ΔR|` 算 rs,越高越好,五欄同向。
- **Correlation 在同一支腳本內完成**(Stage 3,零 GPU):抽完特徵後逐 benchmark 算 Spearman + mean 列,直接寫出 `{family}_spearman.md`——不需要第二步驟。

## 4. 預期輸出與 rebuttal 用法

- `out/E1/{family}_metrics.csv` — 每 (variant, benchmark) 的四個 metric + `iso_dev` + |ΔR|
- `out/E1/{family}_spearman.md` — per-benchmark rs 表 + mean 列 + **E8 節**(per-variant iso_dev 表,填 pCi8-W6 的 [TBD-E8])
- E1 的 log 另含 `[load]`/per-bench 計時行,供 **E12** 成本表收割

Rebuttal 表格形式(G3T9-W3、新 Table):

> mean Spearman across PTQ cells: PRISM B [paper: 0.820]、1−CKA [XX]、1−SVCCA [XX]、Procrustes dist [XX]

並補一句定性論證(不管數字誰高都成立):即使在 rs 接近的 cell,CKA/SVCCA **沒有 risk 上界、沒有三軸歸因**,無法產生 Sec. 5.3 的診斷;完整 bound 是「為什麼是 Ω 而不是 CKA」的推導來源。

> **完整明細表(含 Ω_I/Ω_W 兩 gauge、成分機制表、子群、bootstrap)在
> [`E1.exp.md`](E1.exp.md)——由 `exp_e1_report.py` 自動生成,勿手改;
> REDO 之後重跑產生器即全表更新。**

## Rebuttal 引用表(rotation-invariant 同級對照;mean rs over 5 benchmarks,n=12/11)

| score(vs \|ΔR\|) | Llama | Qwen3 | Mean r_s |
|---|---|---|---|
| 1−CKA | +0.924 | +0.882 | +0.903 |
| 1−SVCCA | +0.924 | +0.878 | +0.901 |
| 1−Ω_N(相似度核,單獨) | +0.839 | +0.773 | +0.806 |
| **PRISM B_N(certified bound,W_N gauge)** | **+0.927** | **+0.896** | **+0.911** |

出處:`E1.exp.md` §1(自動生成)。註:(1) B_N/Ω_N = 論文 CSV **全量特徵**;
CKA/SVCCA 的 gsm8k 格仍為 16k 子抽樣——**REDO 後該格只會下修,B_N 優勢只增不減**;
(2) bootstrap:B_N−CKA 差距不顯著(llama +0.003 / qwen +0.015,CI 皆蓋 0)——
「統計平手但多一張證書」;(3) gsm8k 機制:1−Ω_N 塌到 +0.48/+0.66 時,B_N 的
排序由 scale 項與 **γ_N**(最優對齊偏離 identity 的 head 表達,App C.2 的
I/N-gap 訊號)攜帶,+0.965/+0.964。已同步進 draft 三處(AC-C/G3T9-W3/pCi8-W3)。

## 4.5 結果與分析(2026-07-25;GPTQ 部分補齊後更新;`out/E1/{family}_spearman.md`)

| family | n | PRISM B | 1-CKA | 1-SVCCA | Procr | 1-Ω_I |
|---|---|---|---|---|---|---|
| llama mean | **11** | +0.829 | +0.920 | +0.922 | +0.921 | +0.915 |
| qwen mean | 10 | +0.782 | +0.855 | +0.857 | +0.855 | +0.857 |

**GPTQ 補齊狀態(2026-07-25 13:07 完成)**:`_load_gptq_with_fallback`(GPTQConfig
覆寫 exllama→v2)成功救回全部三個 repo——**llama 12/12、qwen 11/11,variant 集與
論文完全一致**。⚠️ 舊 `{family}_spearman.md` 仍是 n=11/10 的 stale 版,將於 gsm8k
REDO run 時自動重生(resume+stage-3);**任何引用以最新 CSV 重算為準**。

**1e. Bound-gauge 發現(W=I vs W=W_N;`subgroup_analysis.md` 可重現)**:CSV 本有
`Bound_W` 欄(App C.1 家族,同等 certified;全量特徵)。n=12/11 完整結果——
**B_W mean llama +0.927 / qwen +0.896,兩家皆 ≥ 最強 baseline**(CKA +0.924/+0.882),
含 gsm8k(+0.965/+0.964,全量、無子抽樣問題);B_I 在 extraction-QA 領先
(squad +0.811/+0.955)。機制:gsm8k 上 1-Ω(全量)只有 ~0.5,B_W 的訊號由
**γ_W(最優旋轉偏離 identity 的 head 表達 = isometry-violation 量)**攜帶——
App C.2「I/N gap 即訊號」的實證,與 E8 互鎖。**不變性等級論證**:CKA/SVCCA 是
旋轉不變量,與之同級的家族成員是 B_W(B_I 是座標敏感 gauge)——原比較跨了
不變性等級。Draft 定稿(redo 後)採兩-gauge 框架:相似度 baselines 複製了家族
單一成員的排序(減去 certificate);家族的旋轉不變 gauge 在其主場追平或勝出。

**GPTQ 加入的效應**:llama B 在 arc/mmlu/squad/triviaqa 四個 benchmark 全部上升
(0.891→0.918、0.915→0.936、0.697→0.773、0.988→0.991),唯 gsm8k 下降
(0.612→0.527)——新 variant 在訊號豐富的 benchmark 被 B 排對、在 noise-floor
benchmark 被排錯,與 SNR 敘事一致。

判讀(= §5 預註冊的「CKA 追平」情境,照該計畫處理):

1. **差距 ~92% 來自 gsm8k**(B +0.53/+0.59 vs baselines +0.95)。排除後:llama **+0.90 vs +0.91**(差 0.009,不到一次換位)、qwen +0.83 vs +0.83 **打平**——App F.3 的壓縮-|ΔR| regime,B 的 risk-scale 組成在該處失去排序訊號,純失真單調量仍能追粗粒度 bit 順序(誠實入文,不宣稱 baselines 在 gsm8k 失效)。
   **1a. 新 pattern(GPTQ 補齊後浮現):B 在兩個 extraction-QA benchmark 全勝、只在壓縮-|ΔR| benchmark 落後**——SQuAD(llama +0.77 vs +0.74;qwen +0.94 vs +0.82)與 TriviaQA(llama +0.99 vs +0.98;qwen +0.92 vs +0.87)雙 family 皆勝;落後處只剩多選題(arc/mmlu,小幅)與 gsm8k(noise floor)。對照 E6 的 |ΔR| 動態範圍:triviaqa/squad 最寬、arc/mmlu 壓縮、gsm8k 最小——**B 恰好在 risk 目標訊號最豐富處勝出、只在目標最噪處付 certification 保費**。
   **1c. ⚠️ gsm8k 差距大半是「子抽樣 artifact」(2026-07-25 審計,待重跑修正)**:gsm8k 是唯一被 token_cap=16384 截斷的 benchmark(63k→16k tokens)。決定性證據——**同一個 Ω 統計量,全量特徵(paper-round)算 rs=+0.48/+0.66,16k 子抽樣算 +0.94/+0.96**;B(全量)的 +0.51/+0.68 與全量 Ω 同水準,baselines(子抽樣)的 +0.95 與子抽樣 Ω 同水準——gsm8k 這格是「全量 B vs 子抽樣 baselines」的**不對等比較**。其餘四個 benchmark 無子抽樣,兩輪 Ω 排序 +1.000 完全一致。深層含義:gsm8k 的 variant 間相似度差異本身趨近 tie,排序對估計細節極敏感(App F.3 的另一面)。**修正**:token_cap 預設 → 65536(gsm8k 全量;SVCCA 已加 covariance 路徑防 OOM),`--redo gsm8k` 只重算該 benchmark(~50-70 min/family,模型重載主導)。**重跑前 draft 的 gsm8k 對比句(B +0.53 vs CKA +0.95)不可再引**。
   **1d. Bootstrap:所有差距統計不顯著**(variants 重抽 5000 reps,`subgroup_analysis.md` 可重現):B−CKA all-5 llama −0.097 CI[−0.244,+0.001]、qwen −0.069 CI[−0.228,+0.045];ex-gsm8k 皆 |diff|≤0.016 且 CI 穩穩蓋 0——**n=11/10 的 Spearman 噪音下,「CKA 比 B 好」本身就不成立**,可入 draft。
   **1b. 子群分析證實機制 = Sec 5.5 的 γ 異質混池**(GPTQ 補齊後重跑,結論不變):固定 head 協議後差距基本消失——GGUF-only gsm8k:B +0.83/+0.94(vs CKA +1.00/+0.94);**qwen 的 GGUF-only arc/mmlu/gsm8k,B 與 CKA 逐格精確相等**;llama SQuAD GGUF-only 雙方 +1.00;non-GGUF(llama n=5 / qwen n=4)同水準,llama mmlu/squad 子群內精確打平(0.90/0.90、0.70/0.70)。⇒ pooled 落後純粹是 γ 項在跨協議混池 × 低-|ΔR| benchmark 打亂跨群順序;γ 必須留在 bound 裡保 validity,而它正是 SQuAD 全勝的來源。**Certification–discrimination tradeoff**:排序競賽獎勵丟掉局部不活躍的資訊,upper bound 不能丟。reviewer 若以「CKA 排序較好」進攻:CKA 排序 ≡ Ω 排序(rank-identical 實證)⇒ 還原成 pCi8-W3,已有答案;「拿 CKA 校準」的預答:繼承 head-盲,恰在 SQuAD 型 cell 失效,且無 failure-mode 分析。
2. **SQuAD 的勝差機制 = head 軸**(Sec 5.3 Q6_K 案例;qwen 勝差最大 +0.12)——backbone-only 分數的結構性盲區;llama SQuAD 子群內雙方 +1.00 但 pooled 時 B 反超,γ 把 GGUF/BnB 跨群順序排對。
3. **三個 baseline 互為冗餘**:rank(1−CKA)==rank(procr) 精確成立(已驗證),rs 欄幾乎逐格相同——App B 結構論點的實證版。
4. **n=11/10**:GPTQ 一個 repo 已補回(resume 機制生效),`-gptq-4bit` 與 qwen GPTQ 仍失敗(見上方狀態塊);same-n 設計保住公平。

### E8 postmortem:首版 iso_dev 作廢 → 獨立 lite 腳本

E1 內建的 iso_dev(無約束對齊 A* 的奇異值 CV)**設計錯誤**:answer-only benchmark 的 n_tokens < d=4096(mmlu 僅 ~511),回歸欠定,量到的是特徵譜條件數而非 isometry violation(FP16 控制組 18–50,無 bit 趨勢)。已從 exp_e1 移除。替代:`exp_e8_isometry.py`(top-r 有效秩子空間、兩側各自 PCA 基底保旋轉不變、residual-ratio `iso_gain`、noise-floor 自動旗標;合成資料金測全過:identity/rotation ≈0、各向異性單調)。`script_E8.sh`,~20 min,重用 E1 的 Z_T 快取。

## 5. 風險與 fallback

| 風險 | 處理 |
|---|---|
| CKA 在部分 cell 追平甚至超過 PRISM | 誠實報告 per-cell;論證重心移到 risk-link + attribution(pCi8-W3 草稿的第 (2)(3) 點)。切勿只報 mean 掩蓋 |
| SVCCA 對 PCA 維度敏感 | 已固定 99%/cap256 並寫死在腳本;附錄註明 |
| 某 GPTQ repo 載入失敗(kernel 相依) | 腳本逐 variant try/except、記錄失敗續跑;表格 n 欄反映實際數量 |
| 時間不夠跑 Qwen | Llama 單 family 已可支撐該表;Qwen 標 revision |

## 6. 執行

```bash
# GPU 機器、repo root
bash rebuttal_exp/script_E1.sh                 # 兩個 family 都跑
FAMILIES=llama bash rebuttal_exp/script_E1.sh  # 只跑 Llama(~2 h)
```

### GPTQ 恢復流程(2026-07-25 加入 resume 機制後)

```bash
pip install gptqmodel optimum      # 修 transformers 的 QuantizeConfig NameError
bash rebuttal_exp/script_E1.sh     # 直接重跑即可:resume 會跳過 CSV 已完成的
                                   # 10 個 variant,只載 GPTQ(llama 2 個、qwen 1 個)
                                   # ≈ 15–30 min/family(首次含 GPTQ repo 下載)
```

Resume 語義:讀既有 `{family}_metrics.csv` → 完整 variant 跳過、部分完成的只補缺的
benchmark、舊 iso_dev 欄自動剔除;Stage-3 Spearman 表在合併後的 rows 上重生
(n 10→12/11)。**跑完後 draft 的 E1 數字要重新填**(means 會變),並重跑
`python3 rebuttal_exp/exp_e1_subgroups.py` 更新子群表——把新 CSV 拉回本機交給我即可。
