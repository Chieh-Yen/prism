# E3 — Reference set ablation(size × domain;診斷端 + regularizer 端)

## 1. 目的與回應對象

| Reviewer 點 | 內容 |
|---|---|
| **G3T9-W2** | (1) bound 用 benchmark 本身的輸入計算,能否用 benchmark-independent reference set?(2) 樣本數 ablation |
| **8VrD-Q3 / W3** | shape regularizer 對 32-seq reference set 的 size 與 domain 敏感度 |

## 2. 可行性結論:**可行,且三分之一已經跑完(零 GPU)**

| Part | 內容 | 成本 | 狀態 |
|---|---|---|---|
| A | benchmark-independent reference(用論文 CSV 既有的 wikitext / fineweb_edu rows 交叉關聯) | **零 GPU,已完成** | ✅ `out/E3/partA_cross_reference.md` |
| B | reference **size** ablation {8,16,32,64,128}(wikitext,Llama family 全 variants) | ~1–1.5 h GPU | 待跑 |
| C | **regularizer 端**敏感度:trace λ=1.0、Llama-TruthfulQA、size {8,32,128} × domain {truthfulqa, wikitext} | 6 runs ≈ 2.5 h GPU | 待跑 |

取捨:Part B 只跑 Llama family(代表性);Part C 用 3 個 size 而非 5(端點 + 論文值),domain 2 種。全部合計 **~4 h GPU**。

## 3. Part A 已有結果(誠實呈現,這是這一題的核心態度)

`rs( Bound_I on reference corpus, |dR| on benchmark )`,pooled over 全部 11 個 family × 5 benchmarks(55 cells):

| reference | mean rs | sd |
|---|---|---|
| same-benchmark(論文預設) | **+0.725** | 0.254 |
| wikitext(benchmark-independent) | **+0.506** | 0.254 |
| fineweb_edu(benchmark-independent) | +0.330 | 0.259 |

解讀與 rebuttal 框架(對應 Google Doc G3T9-W2 草稿的「state honestly whether rankings are preserved」):

1. **Generic reference 保留了大部分排序訊號但確有衰減**(0.725 → 0.506)。這不是 bug 而是特性:PRISM 的 bound 是 *input-conditioned* 診斷——「這個 variant 在這種輸入分佈上會退化多少」;拿掉輸入條件自然損失針對性。
2. wikitext 一致優於 fineweb_edu(fineweb 是 streaming 抽樣、domain 更雜)。建議 rebuttal 引 wikitext 數字。
3. per-family 表在輸出檔中;主文 2×5 grid 的兩個 family 單獨引用時請從該檔取值(例:Qwen3-8B 的 triviaqa/gsm8k cells 在 generic reference 下轉負——與 GSM8K 既有的 CE-decoupling 限制一致,pCi8-W4 的敘事可交叉引用)。

## 4. Part B / C 設計

**Part B(診斷端 size)**:對 Llama 全 12 variants,各 size 抽 wikitext 特徵、重算 Bound_I(理論 K 用 target head),對既有 benchmark `|MdR|` 算 rs。輸出 size × benchmark 的 rs 矩陣 → 填 G3T9-W2 草稿的 `{8,16,32,64,128} → [XX/XX/XX/XX/XX], stabilizing at [N]`。
每個 variant 一次載入、5 個 size 順跑(≤128 序列的 forward 是秒級,成本全在載入)。

**Part C(regularizer 端)**:`train_forgetting_baselines.py --method trace --ref_task {truthfulqa|wikitext} --reg_samples {8,32,128}`,輸出與論文同 schema 的 JSON → 用 `exp_e2_aggregate.py` 的 summarize 邏輯比 retention 差異 → 填 8VrD-Q3 草稿的 `changes retention by at most [XX]%`。

## 5. 執行

```bash
bash rebuttal_exp/script_E3.sh                # A+B+C 全跑
PARTS="A" bash rebuttal_exp/script_E3.sh      # 只跑零 GPU 部分(任何機器)
PARTS="B C" bash rebuttal_exp/script_E3.sh    # GPU 機器
```

## 6. 風險與 fallback

| 風險 | 處理 |
|---|---|
| generic-ref 排序衰減被 reviewer 放大 | 框架:同時給 same-bench 與 generic 兩欄,主張「diagnostic 用 same-input(無標籤,依然便宜),screening 用 generic」;論文 claim 收斂為 input-conditioned |
| Part C 顯示 regularizer 對 domain 敏感 | 按 Google Doc:「Report honestly if sensitive」;若 wikitext ref 也有效,反而是加分(regularizer 不需要任務內資料) |
| size=8 不穩定 | 預期內(8 條序列的 Ω 噪音大);敘事:32 已飽和,這正是論文選 32 的理由 |
