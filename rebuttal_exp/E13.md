# E13 — 論文 2×2×5 grid 的完整 20-cell 相關矩陣(零 GPU,已完成)

## 1. 目的與回應對象

| Reviewer 點 | 內容 |
|---|---|
| **8VrD-W4(1)** | 「0.831 只是 Llama grid;Qwen-BBQ 有 −0.34/−0.66,主文聚合應含全部 cells」 |
| **AC-E** | 失敗/mixed cell 要誠實呈現:主文承諾放全 cell 矩陣——**本實驗把承諾變成數字** |

draft 原本只寫「revised main text 會報 20-cell mean/median」(承諾、無數字);E13 用既有 λ=0 JSON 零 GPU 算出實際值,可直接 inline。

## 2. 可行性結論:**零 GPU,已完成**(`script_E13.sh` 秒級可重跑)

輸入:`exp_result/regularization/0.0/{llama,qwen}/prism_forgetting_metrics_{truthfulqa,bbq}.json`(λ=0 no-reg 軌跡,28 checkpoints 到 step 700)。輸出在 `out/E13/`。

## 3. 方法與 checksum(關鍵設計)

每 cell 對訓練 checkpoints 算 `rs(bound_total_t, |delta_risk|_t)`。**先掃 protocol 小空間**(risk key ∈ {answer-only, full} × window ∈ {≤300, all} × 含不含 step 0),用論文已知數字當 checksum:

- **命中**:answer-only `delta_risk`、window ≤300(= 論文 analysis window)之下:
  - Llama 10-cell mean = **+0.830**(論文 0.831,誤差 0.001)
  - Qwen-BBQ 負 cell = **−0.34 / −0.66**(與 Fig 7 完全一致)
- 判定:**REPRODUCED**——本地 λ=0 樹就是論文 Fig 7 那一輪,聚合數字可當論文 grid 自己的數字引用(不受 E10 §4 對 λ>0 trace 輪次的 caveat 影響;該 caveat 仍適用於 E10 的 10-cell 表)。

## 4. 已跑出的結果(可直接填 draft)

| model | FT task | arc | mmlu | squad | triviaqa | gsm8k |
|---|---|---|---|---|---|---|
| llama | truthfulqa | +0.86 | +1.00 | +0.99 | +0.97 | +0.97 |
| llama | bbq | +0.94 | +0.92 | +0.30 | +0.88 | +0.48 |
| qwen | truthfulqa | +0.99 | +0.94 | +0.99 | +0.58 | +0.62 |
| qwen | bbq | +0.94 | **−0.34** | +0.83 | **−0.66** | +0.99 |

**聚合(8VrD-W4(1) / AC-E 引用句)**:

- 全 20 cells:**mean rs +0.71,median +0.93**;負 cell 恰好只有 Qwen-BBQ 的 2 個(mmlu −0.34、triviaqa −0.66),與 App G.1 的 noise-floor 診斷一致
- llama 10 cells:mean **+0.830**;qwen 10 cells:mean **+0.587**
- 18/20 cells 為正;負 cell 都落在 |ΔR|~1e-3 的 noise-floor regime(G.1 L770–782)

Rebuttal 句型:*the full matrix is: mean rs +0.71 / median +0.93 over all 20 (model, FT-task, benchmark) cells, with exactly two negative cells — both Qwen-BBQ, both in the |dR| ≈ 1e-3 noise-floor regime App. G.1 diagnoses.*

## 5. 執行 / 重現

```bash
bash rebuttal_exp/script_E13.sh   # 任何機器,~2 秒
```

輸出:`out/E13/E13_results.md`(含 protocol-scan 表)、`out/E13/matrix.csv`。
