\begin{table}[t]
\centering
\caption{\textbf{Component-wise ablation of the PRISM bound} against the empirical cross-entropy risk gap $|\Delta\mathcal{R}|$, measured by per-cell Spearman rank correlation across the $4{\times}5$ PTQ grid (20 cells); larger $|r_s|$ is uniformly better ($|r_s|{=}1$ = perfect variant ranking). The three rows compose the bound cumulatively---shape, then $+$ scale, then $+$ head---so the row-to-row difference cleanly isolates each component's contribution to ranking signal. The shape-only similarity $\Omega_I$ already gives mean $|r_s|{=}0.77$; adding the scale term to form the feature alignment error $\delta_I{=}K_{\mathrm{feat}}\sqrt{(\Delta\rho)^2{+}2\rho_T\rho_P(1{-}\Omega_I)}$ lifts this to 0.82 and wins the plurality of cells---the largest marginal contribution of any component. Adding the covariance-weighted head term to form the full bound $\mathcal{B}_I$ brings the aggregate mean slightly down to 0.79: our suite mixes protocols that quantize \texttt{lm\_head} (GGUF k-quant tiers, $\gamma{>}0$) with protocols that keep it FP16 (GPTQ and BnB, $\gamma{\equiv}0$), and this heterogeneity injects non-monotone variance into the $\gamma$ term. $\mathcal{B}_I$ nevertheless remains the certified upper bound of Theorem~\ref{thm:unified_bound} and is strictly required when $\gamma{>}0$; it also has the smallest cross-lineage spread (per-lineage std $\approx$ 0.04 vs.\ 0.05--0.06 for $\Omega_I$ and $\delta_I$), consistent with the head term stabilising outlier lineages. ``Wins'' counts cells where the metric's $|r_s|$ is the largest (tied winners counted for every tied metric); bold = best in column.}
\label{tab:baseline}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcccccc}
\toprule
Metric & Mean $|r_s|$ & Llama & Ministral & Qwen3 & DeepSeek & Wins / 20 \\
 & \scriptsize (20 cells) & \scriptsize (5 benchmarks) & \scriptsize (5 benchmarks) & \scriptsize (5 benchmarks) & \scriptsize (5 benchmarks) &  \\
\midrule
$\Omega_I$ \; {\scriptsize (shape only)} & .768 & .825 & .758 & .783 & .707 & 5 \\
$\delta_I$ \; {\scriptsize (+ scale; no head)} & \textbf{.822} & \textbf{.881} & \textbf{.762} & \textbf{.855} & \textbf{.791} & \textbf{10} \\
$\mathcal{B}_I$ \; {\scriptsize (+ head; PRISM, ours)} & .786 & .828 & .753 & .813 & .749 & 9 \\
\bottomrule
\end{tabular}
\end{table}
